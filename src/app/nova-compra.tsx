import { Text, View } from 'react-native';

export default function NovaCompra() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Nova Compra (em construção)</Text>
    </View>
  );
}